# Dense Long Trail Configuration
# Creates a dense, long-lasting cursor trail

# Trail appearance
spriteSize=20.0
texture=cursortrail.png

# Trail behavior - Dense and long-lasting
fadeTime=3.0            # Particles last 3 seconds
fadeRate=0.02           # Very slow fade
spawnFrequency=2.0      # Very dense spawning (every 2 pixels)
maxParticles=6000       # Lots of particles for long trail