# Large Particles Configuration  
# Creates a trail with big, prominent particles

# Trail appearance
spriteSize=40.0         # Large particles
texture=cursortrail.png

# Trail behavior - Balanced for large particles
fadeTime=1.5            # Medium duration
fadeRate=0.04           # Slower fade to show large particles
spawnFrequency=25.0     # Wider spacing for large particles
maxParticles=1500       # Moderate count to avoid overcrowding