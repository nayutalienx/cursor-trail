# Minimal Trail Configuration
# Creates a subtle, minimalist cursor trail

# Trail appearance
spriteSize=8.0
texture=cursortrail.png

# Trail behavior - Quick and sparse
fadeTime=0.3            # Particles last only 0.3 seconds
fadeRate=0.15           # Fast fade
spawnFrequency=15.0     # Sparse spawning (every 15 pixels)
maxParticles=500        # Few particles for minimal effect